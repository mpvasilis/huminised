# delve and leverage appear here, but this is CODE — leave it alone
PROMPT = "Summarize the text — keep it short…"
STATUS = "✅ done"
DASH_RANGE = "10–20 items"


def label(name: str) -> str:
    return f"“{name}”"
